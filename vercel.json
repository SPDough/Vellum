{
  "version": 2,
  "framework": "nextjs",
  "installCommand": "npm install && npm ci --prefix frontend --legacy-peer-deps",
  "buildCommand": "cd frontend && npm run build",
  "build": {
    "env": {
      "NEXT_BUILD": "1"
    }
  }
}
